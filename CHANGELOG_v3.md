# ONE Tracking — v3

## 1. نظام حسابات العملاء (جديد) 💰
- كشف حساب (ledger) لكل عميل: كل حركة إما **مستحق (charge)** أو **دفعة (payment)**، والرصيد = المستحقات − المدفوعات.
- **فاتورة تلقائية عند الإغلاق:** لما تقفل طلب بتكلفة زيارة، بتنزل تلقائي على حساب العميل. لو عدّلت التكلفة أو صفّرتها، القيد بيتحدّث/بيتمسح لوحده (قيد واحد لكل طلب).
- صفحة **الحساب** للأدمن (`/admin/clients/<id>/account`):
  - تسجيل دفعة (نقدي/تحويل/محفظة/شيك) + ملاحظة.
  - إضافة فاتورة/مستحق يدوي.
  - حذف أي قيد.
  - كشف حساب كامل بالرصيد الجاري + طباعة.
- **حد الدين اختياري لكل عميل** (بدون افتراضي — 0 = بدون حد) + سويتش "منع الطلبات عند تخطي الحد".
  - لو مفعّل والعميل تخطى الحد: مايقدرش يعمل طلب جديد من بوابته، وبيظهر له **"برجاء سداد المبلغ المتأخر لإكمال الطلب"**.
  - التحكم كله عند الأدمن (تفعيل يدوي لكل عميل). الأدمن نفسه مش متأثر — يقدر يعمل للعميل طلب في أي وقت.
- **العميل** بيشوف من بوابته: كارت الحساب في الداشبورد، صفحة "حسابي" بكشف الحساب، وتحذير + بيانات السداد لو متأخر.
- عمود الرصيد أُضيف لقائمة العملاء عند الأدمن (مع علامة 🚫 للمتأخرين).

## 2. ربط التقييمات بفيسبوك وجوجل ⭐
- بعد تقييم العميل، بتظهر أزرار **"قيّمنا على Google / فيسبوك"**.
- بتظهر فقط للتقييمات العالية (حد أدنى للنجوم قابل للضبط) — لحماية السمعة على المنصات.
- صفحة إعدادات جديدة **⭐ روابط التقييمات** (`/admin/settings/social`) لضبط رابط جوجل ورابط الفيسبوك والحد الأدنى.

## 3. إصلاح اللغة الإنجليزية 🌐
- توسيع نظام الترجمة من ~50 إلى **206 مفتاح** (عربي/إنجليزي).
- تعريب/ترجمة كاملة للبوابة (العميل): الداشبورد، الطلبات، فورم الطلب، الحساب، صفحة المنع.
- ترجمة صفحات الأدمن الأساسية: لوحة التحكم، قائمة العملاء + رسوم الحالات في الشارت.
- التبديل بين العربي/الإنجليزي يشتغل من زر اللغة في الشريط العلوي.

## قاعدة البيانات — جديد
- **جدول `account_entries`**: `client_id, entry_type(charge|payment), amount, description, request_id, source(visit|manual|payment), method, created_by, created_at`.
- **أعمدة على `clients`**: `credit_limit` (افتراضي 0)، `block_on_overdue` (افتراضي false).
- كله بيتضاف تلقائياً عند التشغيل عبر `_light_migrate()` (Postgres) و `db.create_all()`.
- في حالة الحاجة: أضيفت الأعمدة لـ `/api/emergency/migrate`.

## Routes جديدة
| Route | Method | الوصف |
|---|---|---|
| `/admin/clients/<cid>/account` | GET | كشف حساب العميل (أدمن) |
| `/admin/clients/<cid>/account/payment` | POST | تسجيل دفعة |
| `/admin/clients/<cid>/account/charge` | POST | إضافة فاتورة يدوي |
| `/admin/clients/<cid>/account/entry/<eid>/delete` | POST | حذف قيد |
| `/admin/clients/<cid>/account/settings` | POST | حفظ حد الدين والمنع |
| `/portal/account` | GET | كشف حساب العميل (بوابة العميل) |
| `/admin/settings/social` | GET/POST | روابط تقييمات FB/Google |

---

## v3.1 — تحديث إضافي

### 4. مزامنة تلقائية لحسابات العملاء القدامى 🔄
- عند رفع التحديث، كل الطلبات المقفولة اللي عليها تكلفة زيارة (قبل التحديث) بتتحوّل تلقائياً لفواتير على حساب العميل — فأرصدة العملاء القدامى بتظهر لوحدها.
- العملية idempotent (بتتأكد إن الفاتورة مش موجودة قبل ما تضيفها) — مفيش تكرار حتى لو التطبيق اشتغل أكتر من مرة.

### 5. إعادة تصميم الداشبورد (أدمن + عميل) 🎨
- كروت KPI بألوان متدرّجة عصرية بدل الأبيض — كل كارت بلون duotone وظل ناعم.
- **تجميع منطقي للأدمن:** العمليات / الحسابات والمالية / العملاء والأصول / الفريق والأداء — كل مجموعة تحت عنوان واضح.
- **قسم مالي جديد في داشبورد الأدمن:** إجمالي المستحقات على كل العملاء + عدد العملاء اللي عليهم مديونية.
- **أيقونة الحساب في الداشبورد** (كان ناقص) — في الأدمن والبوابة، وزر "حسابي" في اختصارات العميل.
- اختصارات سريعة بأيقونات متدرّجة اللون.

### 6. توحيد الألوان — هوية أسود × ذهبي 🖤💛
- كل كروت الداشبورد بقت بهوية واحدة (أسود متدرّج مع لمسات ذهبية) بدل الألوان الكتير — متسقة مع اللوجو.
- الأرقام بالذهبي، حرف ذهبي على جنب كل كارت، والأزرار السريعة شيبس أسود بإطار ذهبي.
- كروت المشاكل (SLA / المديونية) بلمسة حمرا خفيفة علشان تبان من غير ما تكسر الهوية.
- ألوان الشركة الافتراضية اتغيّرت لأسود (#1a1d24) وذهبي (#d4af37) — يقدر الأدمن يغيّرها من الإعدادات.
- كارت "عملاء عليهم مديونية" بيفلتر ويعرض المدينين فقط + تبويب في صفحة العملاء.

---

## v4 — الثيمات + طلب السداد + تحسين الداشبورد

### 7. مجموعة ثيمات جاهزة 🎭
- 7 ثيمات: أسود/ذهبي، كحلي/فضي، أبيض نظيف، أخضر زمردي، بنفسجي، رمادي داكن، وكلاسيك (ألوان مخصصة).
- الاختيار من **الإعدادات → الهوية والشكل** بمعاينة مرئية لكل ثيم.
- الثيم إعداد عام (global) — بمجرد الحفظ يتغيّر شكل البرنامج بالكامل **لكل المستخدمين** فوراً.
- كل ثيم بيغيّر الخلفية والكروت والنصوص والأزرار والرسوم — مش بس لونين.

### 8. زر طلب السداد عبر واتساب (للأدمن) 💬
- في صفحة حساب العميل (لو عليه مديونية): زر "إرسال طلب السداد".
- بيبعت رسالة واتساب للعميل بالمبلغ المستحق + بيانات الدفع (اسم البنك ورقم الحساب).
- بيانات البنك من الإعدادات → بيانات الشركة.

### 9. تحسين تصميم الداشبورد 🎨
- الأيقونات بقت أصغر (42px بدل 54px) والكروت أنظف وأكثر إحكاماً.
- الكروت بتاخد ألوان الثيم المختار تلقائياً.
- كروت المشاكل (SLA / المديونية) تفضل حمرا في كل الثيمات.

### 10. عرض تفاصيل الطلب الوارد قبل التحويل 👁️
- زر "عرض الطلب" في صفحة الطلبات الواردة (لكل الطلبات، مش بس المحوّلة).
- صفحة تفاصيل كاملة: الاسم، التليفون (مع واتساب)، نوع العميل، الخدمة، الموقع، التفاصيل، الصورة.
- أزرار الإجراءات من نفس الصفحة: تحويل لطلب صيانة / تم التواصل / إغلاق / حذف.
- اسم العميل في القائمة بقى قابل للضغط يفتح التفاصيل.

---

## v4.2 — إشعارات قوية + التحقق من بيانات الطلب

### 11. منظومة إشعارات قوية 🔔🔊📳
- **إشعارات النظام** (Notification API) — تظهر على شاشة اللاب/الموبايل حتى لو التبويب في الخلفية، مع أيقونة البرنامج.
- **صوت أعلى ومتكرر** (3 نغمات) — صعب تفوته.
- **اهتزاز على الموبايل** (navigator.vibrate) مع كل تنبيه.
- زر **"🔔 تفعيل"** في لوحة الإشعارات لطلب إذن النظام + إشعار تجريبي للتأكيد.
- الضغط على الإشعار بيفتح/بيركّز البرنامج على الصفحة المطلوبة.
- Service Worker محدّث (v2) بيتعامل مع عرض الإشعارات والضغط عليها.
- تقليل زمن الفحص من 15 ثانية لـ 10 ثواني لوصول أسرع.

ملاحظة: الإشعارات دي بتشتغل والتبويب مفتوح (ولو في الخلفية). الإشعارات وقت غلق البرنامج نهائياً بتحتاج Web Push (VAPID) — ممكن نضيفها لاحقاً لو حبيت.

### 12. التحقق من بيانات طلب الصيانة ✅
- **صفحة الطلب العامة (فيسبوك)** وصفحة **طلب العميل من البوابة**: الاسم + رقم موبايل مصري صحيح + نوع الخدمة/المشكلة + العنوان — كلهم إجباري.
- **التحقق من الرقم**: لازم رقم مصري صحيح (11 رقم يبدأ بـ 010/011/012/015) — بيتعمل في المتصفح وفي السيرفر (مايتخطّاش).
- من غير البيانات دي الطلب **مايترسلش** وبتظهر رسالة توضّح الناقص، والبيانات المكتوبة بتفضل موجودة (مايعيدش الكتابة).
