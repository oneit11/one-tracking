# ONE Tracking - النسخة المحدثة v2

## أهم التعديلات

### 1. داشبورد جديد عصري
- كروت KPI بأيقونات وألوان — كل كارت clickable ويودّيك للصفحة المفلترة
- شريط اختصارات سريعة (طلب / تذكرة / عميل / جهاز / مستخدم / QR / تقارير / إعدادات)
- رسومات أوضح (Chart.js) ملاءمة للـ RTL
- Portal (العميل) وTech (الفني) بنفس التصميم الجديد

### 2. الأيقونات كلها Clickable
- كل كارت في الداشبورد بيوديك للصفحة الخاصة به مع الفلتر المناسب
- الصور المرفوعة (صور العميل + صور التقارير + صور القرآت) بتفتح lightbox بحجم كامل بضغطة
- ESC أو ضغطة برة الصورة تقفلها

### 3. جدولة مواعيد المتابعة (Follow-up) ⭐ جديد
- عند وجود تقرير زيارة، يظهر قسم "مواعيد المتابعة" في صفحة الطلب (أدمن)
- الأدمن يقدر يجدول موعد + يعيّن فني + يكتب ملاحظات
- الفني يشوف المواعيد المجدولة له في داشبورده
- تعليم "تم" أو "إلغاء" للأدمن
- الفني كمان يقدر يجدول متابعة من صفحة رفع التقرير

### 4. الحذف من الأدمن فقط 🔒
تم إضافة أزرار حذف (🗑️) للأدمن فقط في:
- المستخدمين (`/admin/users`) — الأدمن ما يقدرش يحذف نفسه
- الطلبات (`/admin/requests`) — بيحذف التعليقات والمتابعات والتقرير معاه
- التذاكر (`/admin/tickets`) + زرار إغلاق سريع
- العملاء (`/admin/clients`)
- الأجهزة (`/admin/devices`) — بيفك ربط الـ QR
- كل عملية حذف بتاكيد + تسجل في Audit Log

### 5. إخفاء المغلقات من الفني والعميل
- **الفني:** الداشبورد بيعرض المفتوحة فقط (assigned / in_progress / report_ready) + المتابعات — لما الأدمن يغلق الطلب يختفي من عند الفني تلقائياً
- **العميل:** لو ضغط على طلب مغلق يرجع لتاب "المغلقة" — الداشبورد و"الطلبات" عندهم يعرضوا المفتوحة افتراضياً + تاب منفصل للمغلقة والكل

### 6. عرض الصور المرفوعة
- كل الصور اللي بترتفع (من العميل + من الفني في التقرير) بتظهر في صفحة الطلب
- كل صورة الآن clickable وبتفتح Lightbox

### 7. إصلاحات backend
- Migration خفيف عند التشغيل بيتأكد من وجود جدول followups
- تنظيف علاقات لما تحذف مستخدم (بيفك الربط بدل ما يعمل orphan)
- زرار "إغلاق تذكرة" اللي كان ناقص

## API/Routes الجديدة

| Route | Method | الوصف |
|---|---|---|
| `/admin/requests/<rid>/followups/new` | POST | جدولة موعد متابعة |
| `/admin/followups/<fid>/done` | POST | تعليم موعد كمنجز |
| `/admin/followups/<fid>/cancel` | POST | إلغاء موعد |
| `/admin/users/<uid>/delete` | POST | حذف مستخدم |
| `/admin/requests/<rid>/delete` | POST | حذف طلب |
| `/admin/tickets/<tid>/delete` | POST | حذف تذكرة |
| `/admin/tickets/<tid>/close` | POST | إغلاق تذكرة |
| `/admin/clients/<cid>/delete` | POST | حذف عميل |
| `/admin/devices/<did>/delete` | POST | حذف جهاز |
| `/portal/requests?show=open|closed|all` | GET | فلترة العميل للطلبات |

## نموذج قاعدة بيانات جديد

**Followup**
- request_id, scheduled_at, technician_id, notes, status, created_by, created_at, done_at
- ينشأ تلقائياً عند أول تشغيل عبر `_light_migrate()`
